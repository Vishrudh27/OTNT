# Server interface base; PrivateKey will be injected
[Interface]
Address = 10.77.0.1/32
ListenPort = 51820
# Optional routing hooks:
# PostUp = sysctl -w net.ipv4.ip_forward=1
# PostDown = sysctl -w net.ipv4.ip_forward=0
